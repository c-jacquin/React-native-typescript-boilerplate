import React, { StatelessComponent } from 'react'
{{#if wantMessages}}
import { FormattedMessage } from 'react-intl'
{{/if}}
import { View } from 'react-native'
import { connect, MapDispatchToProps, MapStateToProps } from 'react-redux'
import { bindActionCreators, Dispatch } from 'redux'

import { AppState, ReduxAction } from 'store/types'
{{#if includedNavigator}}
import Navigator from './{{ name }}.nav'
{{/if}}
{{#if wantMessages}}
import messages from './messages'
{{/if}}
import { {{ pascal name }}Props, {{ pascal name }}ConnectedProps, {{ pascal name }}ActionCreators } from './types'

const {{ pascal name }}: StatelessComponent<{{ pascal name }}Props> = ({

}) => (
    {{#if includedNavigator}}
    <Navigator/>
    {{else}}
    <View/>
    {{/if}}
)

const mapStateToProps: MapStateToProps<
    {{ pascal name }}ConnectedProps,
    {{ pascal name }}Props
> = (state: AppState) => ({})

const mapDispatchToProps: MapDispatchToProps<
    {{ pascal name }}ActionCreators,
    {{ pascal name }}Props
> = (dispatch: Dispatch<ReduxAction>) => bindActionCreators({}, dispatch)

export default connect<ConnectedProps, {{ pascal name }}ActionCreators, {{ pascal name }}Props>(
    mapStateToProps,
    mapDispatchToProps
)({{ pascal name }})
