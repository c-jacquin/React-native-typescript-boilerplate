import React, { PureComponent, PropTypes } from 'react'
{{#if wantMessages}}
import { FormattedMessage } from 'react-intl'
{{/if}}
import { View } from 'glamorous-native'

{{#if wantMessages}}
import messages from './messages'
{{/if}}
import { {{ pascal name }}Props, {{ pascal name }}State } from './types'

class {{ pascal name }} extends PureComponent<{{ pascal name }}Props, {{ pascal name }}State> {

    render() {
        return (
            <View/>
        )
    }
}

export default {{ pascal name }}
