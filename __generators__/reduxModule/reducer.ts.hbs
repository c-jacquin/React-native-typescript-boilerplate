import { Action, Reducer } from 'redux'

import config from 'config'
import { I{{ properCase name }}State } from './types'

const initialState: I{{ properCase name }}State = {

}

const {{ name }}Reducer: Reducer<I{{ properCase name }}State> = (state = initialState, action: Action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default {{ name }}Reducer
