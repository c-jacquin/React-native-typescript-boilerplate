import { Epic } from 'redux-observable'
import { Observable } from 'rxjs'

import config from 'config'
import { AppState, ReduxAction, EpicDependancies } from '../types'
import * as {{ name }}Actions from './actions'


const {{ name }}Epic: Epic<ReduxAction, AppState, EpicDependancies> = (action$, store, {{#if api}}{ {{name}}Api }{{/if}}) => {
    return action$.ofType('SOMETHING')
}

export default {{ name }}Epic
