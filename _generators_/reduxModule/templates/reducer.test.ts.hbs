import {{ name }}Reducer from '../reducer'
import * as {{ name }}Actions from '../actions'
import appState from 'store/_helpers_/initialState'

describe('{{ name }} reducer', () => {
    it('should return the initial state', () => {
        expect(
            {{ name }}Reducer(appState.{{ name }}, {
                type: 'TEST',
            })
        ).toEqual(appState.{{ name }})
    })
})
