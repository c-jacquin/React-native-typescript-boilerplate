import { Action, Reducer } from 'redux'

import { {{ pascal name }}State } from './types'

export const initialState: {{ pascal name }}State = {

}

const {{ name }}Reducer: Reducer<{{ pascal name }}State> = (state = initialState, action: Action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default {{ name }}Reducer
