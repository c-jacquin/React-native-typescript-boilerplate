import React, { Component, PropTypes } from 'react'
{{#if wantMessages}}
import { FormattedMessage } from 'react-intl'
{{/if}}
import { View } from 'react-native'

{{#if wantMessages}}
import messages from './messages'
{{/if}}
import { {{ properCase name }}Props, {{ properCase name }}State } from './types'

class {{ properCase name }} extends Component<{{ properCase name }}Props, {{ properCase name }}State> {

    render() {
        return (
            <View/>
        )
    }
}

export default {{ properCase name }}
